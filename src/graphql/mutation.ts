// import connection from '../db/env.config';
// import User from '../entities/User';

export default {
  // createUser: async (root, args, context) => {
  //   // const user = await connection(User).
  // },
};
